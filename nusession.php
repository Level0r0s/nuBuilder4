<?php

    session_start();

    // TODO - Implement this to send user to a login page if they have been timed out etc

    if(array_key_exists('nu_user_id', $_SESSION)){
        if($_SESSION['nu_user_id'] == ''){

        }
    }

?>